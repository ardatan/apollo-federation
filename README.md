# Apollo Federation Example Using graphql-middleware

Run Apollo Federation Subgraph with Apollo Gateway using graphql-middleware.

Start Gateway and Service:

```bash
npm start
```

Then visit `http://localhost:4000`
Service runs on `http://localhost:4001`
